# Wokki Client — Claude Code

@AGENTS.md

Branch workspace rule lives in AGENTS.md: Org Admin sees all chi nhánh trong org; Manager scoped; PlatformOperator on `/platform` only; users without Active membership gated to `/join` or `/pending`.
