/** Shared fixed height when employee create dialog is paired with workspace. */
export const EMPLOYEE_CREATE_PAIR_HEIGHT_CLASS = "h-[min(640px,calc(100dvh-2rem))]";

/** Max width of the paired row on desktop. */
export const EMPLOYEE_CREATE_PAIR_MAX_WIDTH_CLASS = "max-w-[min(1100px,calc(100vw-2rem))]";

/** Department workspace — 65% of pair row. */
export const WORKSPACE_PANEL_WIDTH_CLASS = "min-w-0 flex-[65] basis-0";

/** Add employee panel — 35% of pair row. */
export const EMPLOYEE_PANEL_WIDTH_CLASS = "min-w-0 flex-[35] basis-0";
