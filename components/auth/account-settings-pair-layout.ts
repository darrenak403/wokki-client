/** Shared layout when account settings + payment profile are shown side-by-side. */
export const SETTINGS_PAIR_HEIGHT_CLASS = "h-[min(680px,calc(100dvh-2rem))]";

export const SETTINGS_PAIR_MAX_WIDTH_CLASS = "max-w-[min(1200px,calc(100vw-2rem))]";

/** Settings panel — 65% of pair row. */
export const SETTINGS_PAIR_MAIN_PANEL_CLASS = "min-w-0 flex-[65] basis-0";

/** Payment / bank panel — 35% of pair row. */
export const SETTINGS_PAIR_PAYMENT_PANEL_CLASS = "min-w-0 flex-[35] basis-0";

export const SETTINGS_PAIR_GAP_CLASS = "gap-4";

export const SETTINGS_PAIR_PANEL_SURFACE_CLASS =
  "relative flex flex-col overflow-hidden rounded-2xl bg-white shadow-xl ring-1 ring-neutral-200/80 dark:bg-neutral-950 dark:ring-neutral-800";
